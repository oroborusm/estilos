<%= form_for(@fotito, :multipart => true) do |f| %>
  <% if @fotito.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fotito.errors.count, "error") %> prohibited this fotito from being saved:</h2>

      <ul>
      <% @fotito.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :imagen %><br>
    <%= f.file_field :imagen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
